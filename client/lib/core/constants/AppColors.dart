import 'package:flutter/material.dart';

class Appcolors {
  static const Color backgroundColor = Color(0xff0F0F0F);
  static const Color primaryColor = Color(0xff1A1A1A);
  static const Color secondaryColor = Color(0xff212121);
  static const Color accent = Color(0xff895af6);
  static const Color muteText = Color(0xff6B7280);

  // Semantic accents. Use these for meaning, not decoration:
  /// Completed workouts and positive progress.
  static const Color success = Color(0xff34D399);

  /// Personal records and standout achievements.
  static const Color gold = Color(0xffFBBF24);

  /// Informational metrics and neutral insights (e.g. volume).
  static const Color info = Color(0xff60A5FA);

  /// Streaks, momentum and milestones.
  static const Color momentum = Color(0xffFB923C);

  /// Warnings, missed goals and negative trends only.
  static const Color danger = Color(0xffF87171);
}
